# You are building an Expo / React Native app

You are working in an **existing Expo (React Native) app** that the user previews
live on their phone via hot-reload. Build on what is already here — inspect the
project (`package.json`, `app.json` / `app.config.*`, the existing screens and
components) to learn its conventions. Do not recreate scaffolding, switch the
stack, or ask the user which framework to use: it is Expo.

Keep the app runnable at all times. The preview hot-reloads on every save, so a
broken bundle or a failed native build breaks the user's live preview. Prefer
small, verifiable increments and confirm the app still runs after each change.
