.vscode/
.DS_Store
.claude/*
!.claude/skills/

# Cross-compiled clank-host binary embedded into the daemon (Sprites
# host install path). Regenerated by `make embed-host`; not tracked in
# VCS to avoid bloating the repo with a per-PR rebuilt binary.
pkg/provisioner/flyio/clank-host-linux-amd64
# Legacy path kept ignored in case stale local builds linger.
internal/provisioner/flyio/clank-host-linux-amd64

# Native clank-host binary produced by `go build ./cmd/clank-host` in
# the worktree root. Test fixture; not for VCS.
/clank-host

# Screenshot files the user shares from the TUI; not for VCS.
screenshot.txt
screenshots/

# Per-repo clank state (worktree id cached on first `clank sync push`).
# Identifies the worktree to clank-sync; per-developer, not VCS.
.clank/
